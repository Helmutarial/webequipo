"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useEffect, useState } from "react";
import { useAuth } from "@/components/auth-context";
import { useTeam } from "@/components/team-context";
import { useNews } from "@/components/news-context";
import { NewsItem } from "@/lib/news";
import { Player } from "@/lib/team";

const blankNews = (): NewsItem => ({ id: "", title: "", slug: "", excerpt: "", content: "", tag: "CLUB", date: new Date().toISOString().slice(0, 10), image: "", accent: "gold", published: true });

export default function AdminPage() {
  const router = useRouter(); const { session } = useAuth(); const { players, updatePlayer } = useTeam(); const { news, refresh, saveNews, createNews, deleteNews } = useNews();
  const [section, setSection] = useState<"players" | "news">("players"); const [selected, setSelected] = useState<Player | null>(null); const [selectedNews, setSelectedNews] = useState<NewsItem | null>(null); const [saved, setSaved] = useState(false);
  useEffect(() => { if (!session) router.replace("/iniciar-sesion"); else void refresh(true); }, [router, session]);
  if (!session) return null;
  const save = () => { if (selected) { updatePlayer(selected); setSaved(true); } };
  const updateSelected = (field: keyof Player, value: string | number) => setSelected((current) => current ? { ...current, [field]: value } : current);
  const updateSelectedNews = (field: keyof NewsItem, value: string | boolean) => setSelectedNews((current) => current ? { ...current, [field]: value } : current);
  const saveNewsItem = async () => { if (!selectedNews) return; if (selectedNews.id) await saveNews(selectedNews); else await createNews(selectedNews); setSelectedNews(null); };
  return <main className="content-page shell">
    <div className="admin-heading"><div><span className="section-label">PANEL DE ADMINISTRACIÓN</span><h1>Hola, {session.name}<span>.</span></h1><p>Edita la información pública de la plantilla y del club.</p></div><Link className="text-link" href="/equipo">Ver web pública ↗</Link></div>
    <div className="admin-tabs"><button className={section === "players" ? "active" : ""} onClick={() => setSection("players")}>Jugadores</button><button className={section === "news" ? "active" : ""} onClick={() => setSection("news")}>Noticias</button></div>
    {section === "players" ? <div className="admin-layout"><div className="admin-player-list">{players.map((player) => <button className={selected?.id === player.id ? "admin-player active" : "admin-player"} onClick={() => { setSelected(player); setSaved(false); }} key={player.id}><span className="player-avatar">{player.name.slice(0, 1)}</span><span><strong>{player.name}</strong><small>{player.number ? `#${player.number}` : "Sin dorsal"} · {player.position}</small></span><b>Editar</b></button>)}</div>{selected ? <section className="edit-card"><span className="section-label">EDITANDO JUGADOR</span><h2>{selected.name}</h2><div className="edit-grid"><label>Nombre<input value={selected.name} onChange={(e) => updateSelected("name", e.target.value)} /></label><label>Alias<input value={selected.alias} onChange={(e) => updateSelected("alias", e.target.value)} /></label><label>Dorsal<input type="number" value={selected.number} onChange={(e) => updateSelected("number", Number(e.target.value))} /></label><label>Posición<input value={selected.position} onChange={(e) => updateSelected("position", e.target.value)} /></label><label>Goles<input type="number" value={selected.goals} onChange={(e) => updateSelected("goals", Number(e.target.value))} /></label><label>Asistencias<input type="number" value={selected.assists} onChange={(e) => updateSelected("assists", Number(e.target.value))} /></label><label>Partidos<input type="number" value={selected.appearances} onChange={(e) => updateSelected("appearances", Number(e.target.value))} /></label><label>Minutos<input type="number" value={selected.minutes} onChange={(e) => updateSelected("minutes", Number(e.target.value))} /></label><label>Titularidades<input type="number" value={selected.starterAppearances} onChange={(e) => updateSelected("starterAppearances", Number(e.target.value))} /></label><label>Suplencias<input type="number" value={selected.substituteAppearances} onChange={(e) => updateSelected("substituteAppearances", Number(e.target.value))} /></label><label>MVP conseguidos<input type="number" value={selected.mvpCount} onChange={(e) => updateSelected("mvpCount", Number(e.target.value))} /></label><label>Foto (URL)<input value={selected.photo} onChange={(e) => updateSelected("photo", e.target.value)} placeholder="https://..." /></label><label className="full-field">Descripción<textarea value={selected.bio} onChange={(e) => updateSelected("bio", e.target.value)} /></label></div><button className="save-button" onClick={save}>Guardar cambios</button>{saved && <span className="saved-notice">Cambios guardados.</span>}</section> : <section className="edit-empty"><span>←</span><p>Selecciona un jugador para editar su ficha.</p></section>}</div> : <div className="admin-layout news-admin-layout"><div className="admin-player-list news-list"><button className="save-button news-new-button" onClick={() => setSelectedNews(blankNews())}>+ Nueva noticia</button>{news.map((item) => <button className={selectedNews?.id === item.id ? "admin-player active" : "admin-player"} onClick={() => setSelectedNews(item)} key={item.id}><span className="player-avatar">{item.title.slice(0, 1)}</span><span><strong>{item.title}</strong><small>{item.published ? "Publicada" : "Borrador"} · {item.date}</small></span><b>Editar</b></button>)}</div>{selectedNews ? <section className="edit-card"><span className="section-label">EDITANDO NOTICIA</span><h2>{selectedNews.id ? selectedNews.title : "Nueva noticia"}</h2><div className="edit-grid"><label className="full-field">Título<input value={selectedNews.title} onChange={(e) => updateSelectedNews("title", e.target.value)} /></label><label>Etiqueta<input value={selectedNews.tag} onChange={(e) => updateSelectedNews("tag", e.target.value)} /></label><label>Fecha<input type="date" value={selectedNews.date} onChange={(e) => updateSelectedNews("date", e.target.value)} /></label><label>Estilo<select value={selectedNews.accent} onChange={(e) => updateSelectedNews("accent", e.target.value)}><option value="gold">Dorado</option><option value="dark">Oscuro</option><option value="cream">Crema</option></select></label><label className="full-field">Resumen<textarea value={selectedNews.excerpt} onChange={(e) => updateSelectedNews("excerpt", e.target.value)} /></label><label className="full-field">Contenido<textarea rows={8} value={selectedNews.content} onChange={(e) => updateSelectedNews("content", e.target.value)} /></label><label className="full-field">Imagen (URL)<input value={selectedNews.image} onChange={(e) => updateSelectedNews("image", e.target.value)} placeholder="https://..." /></label><label className="checkbox-field"><input type="checkbox" checked={selectedNews.published} onChange={(e) => updateSelectedNews("published", e.target.checked)} /> Publicar noticia</label></div><button className="save-button" onClick={saveNewsItem}>Guardar noticia</button>{selectedNews.id && <button className="delete-button" onClick={async () => { await deleteNews(selectedNews.id); setSelectedNews(null); }}>Eliminar</button>}</section> : <section className="edit-empty"><span>←</span><p>Selecciona una noticia o crea una nueva.</p></section>}</div>}
  </main>;
}
