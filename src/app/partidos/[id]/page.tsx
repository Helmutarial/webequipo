"use client";

import Link from "next/link";
import { useParams } from "next/navigation";
import { useMatches } from "@/components/matches-context";
import { useTeam } from "@/components/team-context";

const eventIcon = { goal: "⚽", substitution: "↕", yellow: "▰", mvp: "★" };
const eventLabel = { goal: "Gol", substitution: "Cambio", yellow: "Tarjeta", mvp: "MVP" };
const formatDate = (date: string) => new Date(date).toLocaleDateString("es-ES", { weekday: "long", day: "numeric", month: "long", year: "numeric" });
export default function MatchPage() { const { id } = useParams<{ id: string }>(); const { matches, loading } = useMatches(); const { players } = useTeam(); const match = matches.find((item) => item.id === id); const playerName = (playerId: string) => players.find((player) => player.id === playerId)?.name || playerId; if (loading) return <main className="content-page shell"><p className="muted">Cargando partido…</p></main>; if (!match) return <main className="content-page shell"><h1>Partido no encontrado<span>.</span></h1><Link className="text-link" href="/partidos">Volver a partidos</Link></main>; const goals = match.events.filter((event) => event.type === "goal"); const substitutions = match.events.filter((event) => event.type === "substitution"); const mvp = match.events.find((event) => event.type === "mvp"); return <main className="content-page shell match-detail"><Link className="text-link" href="/partidos">← Volver a partidos</Link><header className="match-detail-header"><span className="section-label">{match.competition} · {formatDate(match.date)}</span><div className="match-scoreboard"><div><strong>Aldapan Gora</strong><small>ALG</small></div><b>{match.homeScore} — {match.awayScore}</b><div><small>{match.opponentShort}</small><strong>{match.opponent}</strong></div></div><p>{match.venue} · {match.status === "finished" ? "Finalizado" : "Próximo partido"}</p></header><div className="match-detail-grid"><section className="match-detail-card"><div className="detail-card-heading"><h2>Eventos del partido</h2><span>{match.events.length} acciones</span></div><div className="timeline">{match.events.filter((event) => event.type !== "mvp").map((event, index) => <div className="timeline-row" key={`${event.minute}-${index}`}><time>{event.minute}'</time><i className={`event-icon ${event.type}`}>{eventIcon[event.type]}</i><div><strong>{eventLabel[event.type]}</strong><span>{event.type === "substitution" ? `${playerName(event.player)} entra por ${playerName(event.relatedPlayer || "")}` : event.type === "goal" ? playerName(event.player) : event.detail || playerName(event.player)}</span></div></div>)}</div>{mvp && <div className="mvp-callout"><span>★</span><div><small>JUGADOR DEL PARTIDO</small><strong>{playerName(mvp.player)}</strong></div></div>}</section><section className="match-detail-card"><div className="detail-card-heading"><h2>Alineación titular</h2><span>{match.starters.length} jugadores</span></div><div className="lineup-list">{match.starters.map((playerId, index) => <div key={playerId}><b>{index + 1}</b><span>{playerName(playerId)}</span><small>{index === 0 ? "POR" : index < 5 ? "DEF" : index < 8 ? "MED" : "ATA"}</small></div>)}</div><h3 className="bench-title">Suplentes</h3><div className="lineup-list bench">{match.substitutes.map((playerId) => <div key={playerId}><b>—</b><span>{playerName(playerId)}</span><small>SUP</small></div>)}</div></section></div><section className="match-detail-card scorers-card"><div className="detail-card-heading"><h2>Goles</h2><span>{goals.length} goles registrados</span></div><div className="scorers-grid">{goals.map((goal, index) => <div key={`${goal.minute}-${index}`}><time>{goal.minute}'</time><strong>{playerName(goal.player)}</strong><span>{goal.detail}</span></div>)}</div><h3 className="bench-title">Cambios realizados</h3><div className="substitution-list">{substitutions.map((event, index) => <div key={`${event.minute}-${index}`}><time>{event.minute}'</time><span><b>{playerName(event.player)}</b> por {playerName(event.relatedPlayer || "")}</span></div>)}</div></section></main>; }
